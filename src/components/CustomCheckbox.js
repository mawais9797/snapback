import { Box, Checkbox } from "@mui/material";
import React from "react";

const CustomCheckbox = ({ style }) => {
  return (
    <>
      <Checkbox style={style} />
    </>
  );
};

export default CustomCheckbox;
